import React from 'react'
import { View } from 'react-native'
import Header from '../../screen/components/Header'

export const Glicemia = () => {
  return (
    <View>
        <Header titulo='Glicemia' voltar />
    </View>
  )
}
